# 🔥 КРИТИЧЕСКИ ВАЖНО: Настройка Firebase Storage Rules

## ❌ Проблема: Ошибка CORS при загрузке

Ошибка CORS означает, что **правила безопасности Firebase Storage блокируют запрос**.

## ✅ Решение: Настройте правила безопасности

### Шаг 1: Откройте Firebase Console
1. Перейдите на https://console.firebase.google.com/
2. Войдите в свой аккаунт Google
3. Выберите проект **"youtube"** (или проект с ID `fir-7bb8f`)

### Шаг 2: Настройте Storage Rules

1. В левом меню найдите **"Storage"** (или "Хранилище")
2. Нажмите на **"Storage"**
3. Перейдите на вкладку **"Rules"** (вверху страницы)
4. **УДАЛИТЕ** все существующие правила
5. **ВСТАВЬТЕ** следующие правила:

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /videos/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

6. Нажмите кнопку **"Publish"** (Опубликовать) в правом верхнем углу
7. **ПОДОЖДИТЕ 1-2 минуты** для применения правил

### Шаг 3: Настройте Firestore Rules

1. В левом меню найдите **"Firestore Database"** (или "Cloud Firestore")
2. Нажмите на **"Firestore Database"**
3. Перейдите на вкладку **"Rules"** (вверху страницы)
4. **УДАЛИТЕ** все существующие правила
5. **ВСТАВЬТЕ** следующие правила:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /videos/{videoId} {
      allow read: if true;
      allow create: if request.auth != null && request.resource.data.userId == request.auth.uid;
      allow update, delete: if request.auth != null && resource.data.userId == request.auth.uid;
    }
    match /comments/{commentId} {
      allow read: if true;
      allow create: if request.auth != null;
    }
    match /users/{userId} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

6. Нажмите кнопку **"Publish"** (Опубликовать)
7. **ПОДОЖДИТЕ 1-2 минуты** для применения правил

### Шаг 4: Проверка

1. **Обновите страницу приложения** (F5 или Ctrl+R)
2. **Выйдите и войдите снова** (чтобы обновить токен авторизации)
3. Попробуйте загрузить файл снова

## 🔍 Как проверить, что правила применены:

1. В Firebase Console перейдите в **Storage > Rules**
2. Убедитесь, что вы видите правила, которые вы только что вставили
3. В правом верхнем углу должно быть написано **"Published"** (Опубликовано)

## ⚠️ Если проблема все еще есть:

1. Убедитесь, что вы **авторизованы** в приложении (есть кнопка выхода в правом верхнем углу)
2. Проверьте консоль браузера (F12) - там не должно быть ошибок авторизации
3. Попробуйте **выйти и войти снова** в приложении
4. Убедитесь, что в Storage Rules используется правильный путь: `videos/{userId}/{allPaths=**}`

## 🆘 Временное решение (только для тестирования):

Если ничего не помогает, можно временно использовать открытые правила (⚠️ **НЕ ИСПОЛЬЗУЙТЕ В ПРОДАКШЕНЕ!**):

**Storage Rules (временные):**
```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

**Firestore Rules (временные):**
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

⚠️ **ВНИМАНИЕ**: Эти правила разрешают всем авторизованным пользователям читать и писать любые данные. Используйте только для тестирования!

